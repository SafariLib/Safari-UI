'use client';
import { styled, Button, css } from '@mui/material';

const MUIButton = styled(Button)(css`
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
`);

export { MUIButton };
