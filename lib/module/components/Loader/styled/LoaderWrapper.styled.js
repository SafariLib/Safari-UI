'use client';
import { styled, css } from '@mui/material';

const LoaderWrapper = styled('div')(() => css`
        display: flex;
        align-items: center;
        justify-content: center;
    `);

export { LoaderWrapper };
