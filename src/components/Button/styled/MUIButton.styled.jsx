'use client';

import { Button, css, styled } from '@mui/material';

export const MUIButton = styled(Button)(css`
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
`);
