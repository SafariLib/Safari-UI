'use client';

import { css, styled } from '@mui/material';

export const LoaderWrapper = styled('div')(
    () => css`
        display: flex;
        align-items: center;
        justify-content: center;
    `,
);
